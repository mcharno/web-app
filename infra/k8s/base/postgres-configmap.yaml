apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-scripts
  namespace: charno-web
  labels:
    app: postgres
data:
  01-schema.sql: |
    -- This will be populated from backend/src/config/schema.sql
    -- You can copy the contents here or use a Kubernetes job to initialize
    -- For now, this is a placeholder
    \echo 'Database initialization scripts should be added here'

  02-seed.sql: |
    -- This will be populated from backend/src/config/seed.sql
    -- You can copy the contents here or use a Kubernetes job to initialize
    \echo 'Seed data scripts should be added here'

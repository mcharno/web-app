# API Configuration
# Backend server URL (default port is 3080)
VITE_API_URL=http://localhost:3080/api

# Mock Mode Configuration
# Set to 'true' to use mock API responses instead of real backend
# By default, we use the real backend with file-based content (no database needed)
VITE_USE_MOCK_API=false
